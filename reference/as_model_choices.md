# Extract model choices from LlmModelsInfo

Returns the \`models\` element from an \`LlmModelsInfo\` object after
validation.

## Usage

``` r
as_model_choices(x)
```

## Arguments

- x:

  An \`LlmModelsInfo\` object.

## Value

Character vector or named list of model choices.
