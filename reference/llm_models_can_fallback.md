# Check whether provider-default model fallback is allowed

Returns whether an \`LlmModelsInfo\` object indicates that selecting no
explicit model can fall back to a provider default.

## Usage

``` r
llm_models_can_fallback(x)
```

## Arguments

- x:

  An \`LlmModelsInfo\` object.

## Value

Logical scalar.
